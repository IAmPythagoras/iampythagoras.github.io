Player.getPlayerPrePullTime()
=============================

Computes and returns the length of the actions taken before the first action with non zero potency. This determines the length in seconds of the prepull.

**Usage**

.. code-block:: python 

    newPlayer.getPlayerPrePullTime()